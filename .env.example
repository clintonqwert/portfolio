# Canonical origin for metadata, JSON-LD, sitemap and robots.
# Required in production: src/lib/seo.ts throws at build time without it, because
# missing canonicals fail silently rather than loudly.
# No trailing slash.
NEXT_PUBLIC_SITE_URL=https://clintonramonida.ca
